import { totalCost } from 'e-commerce/helpers/total-cost';
import { module, test } from 'qunit';

module('Unit | Helper | total cost');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = totalCost([42]);
  assert.ok(result);
});
